# 此文件由系统自动创建，配置项为默认值。可修改本目录下的 .env 文件，以更新默认值。
# DB_DRIVER support: mysql,sqlite3,postgres
DB_DRIVER = "sqlite3"
DB_HOST = "127.0.0.1"
# DB_PORT like: 3306(mysql); 5432(postgres)
DB_PORT = 3306
DB_NAME = "lemocoder"
# DB_USERNAME like: root, postgres
DB_USERNAME = "root"
DB_PASSWORD = "root"
DB_NODE_ID = 1

# Server
SERVER_IP = "0.0.0.0"
SERVER_PORT = 8888
# 一段时间不活跃，自动断开连接
SERVER_DROP_AFTER = 300
# json, protobuf
MSG_FORMAT = "protobuf"
